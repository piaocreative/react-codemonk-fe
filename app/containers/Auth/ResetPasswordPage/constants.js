export const key = 'resetPasswordForm';

const prefix = 'RESET_PASSWORD_FORM_';

export const CHANGE_TOKEN = `${prefix}CHANGE_TOKEN`;
export const CHANGE_PASSWORD = `${prefix}CHANGE_PASSWORD`;
export const CHANGE_CONFIRM_PASSWORD = `${prefix}CHANGE_CONFIRM_PASSWORD`;
export const RESET_PASSWORD = `${prefix}RESET_PASSWORD`;
