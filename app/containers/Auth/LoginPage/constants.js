export const key = 'talentLogin';

const prefix = 'TALENT_LOGIN_FORM_';

export const CHANGE_EMAIL = `${prefix}CHANGE_EMAIL`;
export const CHANGE_PASSWORD = `${prefix}CHANGE_PASSWORD`;
export const LOGIN = `${prefix}LOGIN`;
