/*
 * AdminProxyLogin Messages
 *
 * This contains all the text for the AdminProxyLogin component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'CodeMonk.containers.AdminProxyLogin';

export default defineMessages({
  proxyLoginCTA: {
    id: `${scope}.proxyLoginCTA`,
    defaultMessage: 'Proxy Login',
  },
});
