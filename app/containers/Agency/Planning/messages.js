/*
 * Planning Messages
 *
 * This contains all the text for the Planning component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'CodeMonk.containers.Planning';

export default defineMessages({
  title: {
    id: `${scope}.title.head`,
    defaultMessage: 'Planning',
  },
  metaTitle: {
    id: `${scope}.metaTitle.head`,
    defaultMessage: 'Planning',
  },
});
