/*
 * SortComponent Messages
 *
 * This contains all the text for the SortComponent component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'CodeMonk.containers.SortComponent';

export default defineMessages({
  sort: {
    id: `${scope}.sort`,
    defaultMessage: 'Sort By',
  },
});
